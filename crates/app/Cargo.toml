[package]
name = "fapolicy-app"
description = "Application state management components"
license = "LGPL-3.0-or-later"
version = "0.4.0"
edition = "2018"

[dependencies]
confy = "0.4.0"
directories = "2.0.2"
lmdb-rkv = "0.14.0"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

fapolicy-analyzer = { version = "0.4", path = "../analyzer" }
#fapolicy-api = { version = "0.4.0", path = "../api" }
fapolicy-daemon = { version = "0.4", path = "../daemon" }
fapolicy-rules = { version = "0.4", path = "../rules" }
fapolicy-trust = { version = "0.4", path = "../trust" }
#fapolicy-util = { version = "0.4", path = "../util" }
